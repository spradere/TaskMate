##################################################
# 2 repo workflow github/test codeberg/main
##################################################

Work on test:

git switch test
git pull
# work
git push

When code is clean, push to Codeberg :

git switch main
git merge --ff-only test
git push
git switch test

##################################################
# 2 repo setup github/test codeberg/main
##################################################

# set name email
git config --global user.name "spradere"
git config --global user.email "spradere@pm.me"

# create repo
git remote add codeberg  git@codeberg.org:spradere/TaskMate.git
git remote add github    git@github.com:spradere/TaskMate.git

# create branch & link repo
git switch -c test
git push -u github test

git branch --set-upstream-to=codeberg/main main
git branch --set-upstream-to=github/test test

# delete a remonte / branch
git remote remove github
git push codeberg --delete main_new

# check
git remote -v
git branch -vv

##################################################
# save
##################################################

# normal save :
	git add .
	git commit -m "comment"
	git push

delete local : git branch -d test_branch
delete remote : git push origin --delete test_branch

# list last commits :
	git log --oneline

# list local / remote branches
	git branch -a --sort=-committerdate

# check tag
	git ls-remote --tags origin

# check commits
	git rev-list --count v0.5

# temporary save/restore modified files
	git stash
	git stash pop

##################################################
# update local repo
##################################################

# quick update
git pull

# what will be done
git fetch
git status

# push uncommitted work
git stash
git pull
git stash pop

# full control
git fetch
git diff HEAD origin/main
git merge origin/main

##################################################
# clean history
##################################################
# get repo size
git count-objects -vH

# search in commits, without merge, most add
git log --no-merges --numstat --date=iso-strict --pretty="C %H %ad %s" \
| awk '
  /^[0-9]/ { add += $1; next }
  /^C / {
    if (NR > 1) print add, prev
    prev = $0
    add = 0
  }
  END { print add, prev }
' \
| sort -k1,1nr -k3,3r \
| head -20

# big blob
git rev-list --objects --all \
| git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' \
| awk '$1=="blob"{print $3, $2, $4}' \
| sort -n \
| tail -20

# find related commit
git log --all --find-object=deadbeef...

# delete file in git history
# 1 - clone
cd ~/code/TaskMate
git clone --mirror git@codeberg.org:Doul09/TaskMate.git taskmate-commit-clean
cd taskmate-commit-clean

# 2 - clean
git filter-repo --path path/file --invert-paths

# 3 - check
git log --stat -- '*file.*'

# 4 - push
git remote add origin git@codeberg.org:Doul09/TaskMate.git
git remote -v
git push --mirror origin

# 5 - check on codeberg

#6 - clone
cd /root/code/TaskMate
mv TaskMate_current TaskMate_old_after_rewrite
git clone git@codeberg.org:Doul09/TaskMate.git TaskMate_current

##################################################
# change user/committer name
##################################################

# 1 test
git config --global --list | grep user
git config --list | grep user
git remote -v
git branch -vv
git log --format="%h %an <%ae> | %cn <%ce>" --all | less

# 2 update
git filter-repo --force --commit-callback '
if commit.author_name == b"Charlie Root":
    commit.author_name = b"spradere"
    commit.author_email = b"spradere@pm.me"
if commit.committer_name == b"Charlie Root":
    commit.committer_name = b"spradere"
    commit.committer_email = b"spradere@pm.me"
'
